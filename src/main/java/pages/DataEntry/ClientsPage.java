package pages.DataEntry;

import org.openqa.selenium.WebDriver;
import utilities.ElementActions;

public class ClientsPage extends ElementActions {

    public  ClientsPage (WebDriver driver) {
        super(driver);
    }
}
